<span class="twitter-typeahead" style="position: relative; display: inline-block;">
  <!-- Make this form a form helper -->
  <form id="search" action="/search" method="GET">
    <input id="spot_type_search_input" class="typeahead tt-query" type="text" placeholder="Search Categories" autocomplete="off" spellcheck="false" dir="auto" name="name">
    <span class="tt-dropdown-menu"></span>
    <button id=  type="submit">Search</button>
  </form>
  <!-- end comment -->
</span>

<div id="results">
  <% if @spot %>
    <% @spot.spots.each do |s| %>
      <p><%= s.name %></p>
      <p><%= s.address %></p>
      <p><%= s.phone %></p>


<div class="spots search-results">
    <div class="map-canvas" data-lat='<%=s.latitude.to_f%>' data-name='<%=@spot.name%>' data-long='<%=s.longitude.to_f%>' style="width:300px;height:250px;margin:0 auto;display:block;"></div>

</div>

    <% end %>
  <% end %>
</div>
